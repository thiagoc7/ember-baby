import { clockDisplay } from '../../../helpers/clock-display';
import { module, test } from 'qunit';

module('Unit | Helper | clock display');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = clockDisplay(42);
  assert.ok(result);
});
